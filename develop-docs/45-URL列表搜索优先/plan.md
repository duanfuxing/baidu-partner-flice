# URL列表搜索优先

后续变更：任务56按用户最新要求改为监听页面点击触发的geturllist响应，并核对响应对应的表格；下文“不监听接口”为历史设计，已被替代。

目标：通过页面URL搜索代替逐页等待目标文本，精确核对搜索结果并进入详情。
范围：流程5搜索、结果等待和行定位；不修改上传、保存、最终审核。
设计：只使用输入框、查询按钮及表格DOM；不调用或监听搜索接口，不读取Vue内部状态。正常页面直接输入URL查询，存在其他筛选才重置。通过表格刷新、加载结束和短暂稳定确认结果；保留精确匹配及无搜索区域旧页面兼容。
步骤：实现DOM刷新等待；修正相似URL误匹配；覆盖延迟加载、重复、空结果和异常；真实只读验证。
依赖：现有Playwright，无新增依赖。
验收：唯一精确结果进入正确详情；重复、空结果、错误提示、不刷新及结果超过一页时停止；新旧详情入口回归通过。
不在范围内：发布安装包、真实业务写入及最终审核。
结果：纯页面实现完成，15项相关Chrome页面测试通过（18.44秒），compileall和git diff --check通过。覆盖旧版原页、新版新标签详情入口。最终真实验收在进入URL列表前被现有登录状态失效阻断（公司搜索提示未登录）；未执行任何上传、保存或最终审核，独立浏览器已关闭。之前接口监听方案的4.27秒验证不代表最终版本真实验收。

限制：最终纯页面代码的真实登录验收待重新登录后执行；无已验证搜索控件的旧页面仍兼容分页；搜索结果超过一页时停止，避免从首页误判唯一；本次未提交代码、未发布安装包。

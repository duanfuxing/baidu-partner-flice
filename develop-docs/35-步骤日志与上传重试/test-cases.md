# 测试场景

正常上传；明确失败后重试成功；3 次重试耗尽；页面已有文件但响应错误；请求未结束；预览未知；本地页面上传、保存、送审回归；日志字段和脱敏。

执行结果：已完成。页面夹具共 46 项通过（完整回归 45 项中旧版域名夹具修正后单项复验通过，另加未完成请求保护 1 项通过）。相关非浏览器回归通过。当前文件卡片不能确认或请求仍执行时停止；只对明确缺失且请求结束的上传重试，首次加 3 次重试。未连接真实送审接口。

# 测试用例

- 三个不同文件依次上传、回填及保存。
- 三个预览但前两张ID相同，必须失败。
- 延迟回调、保存后刷新覆盖数据，不能误报成功。
- 同文件允许在不同业务/卡片合法复用。
- 失败不重传，最终审核关闭。

226项完整回归通过；目标卡片真实修复及关闭浏览器后重新打开的纯只读哈希核验通过，三个文件一一对应且无重复。

补充：后台页面焦点与文件身份相关7项页面测试通过；保存前后内容核验通过；最终审核未触发。

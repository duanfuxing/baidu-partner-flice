# 任务 41：上传重复与预览缺失修复

## 目标与范围
修复成功后因预览未刷新而重传，补强新版回填及保存后核验。依赖任务 23、24、35，无新增依赖。

## 设计和步骤
1. 区分成功、明确失败及结果未知；成功或部分成功禁止重传，预览不能替代成功响应。
2. 沿用当前一个资质目录对应一张卡片的结构；目录内文件改为逐个上传并等待回填，避免批量回调竞争。
3. 保存后及最终集合核验重新确认预览数量，防止刷新丢图仍成功。
4. 本地 HTML + 无头 Chrome 验证延迟回填、重复、部分成功及保存后缺图。

## 验收标准
成功未回填不重传；逐文件验证后继续；结果未知停止；保存后缺图不继续或送审；相关回归通过。

## 不在范围内
发布、修改输入、清理真实草稿、重试保存或最终送审、修复百度服务器/CDN。

## 状态
本地修复完成。全量回归 214 项通过（142.91 秒）；随后新增的预览区域缺失参数化用例共 2 项补验通过。git diff --check 通过。

Windows 真实运行尚未验收；未连接真实上传或送审接口，未打包发布。逐文件上传降低回调竞争风险，但百度图片服务加载失败等外部原因仍需 Windows 现场日志确认。

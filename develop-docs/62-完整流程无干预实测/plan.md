# 完整流程无干预实测

## 目标及范围

按用户要求，使用本机 Chrome 从正式入口执行一次全部输入的真实测试，助手仅观察，不干预执行。

## 固定设置

- 命令：`.venv/bin/python main.py --no-final-submit`。
- 输入：根目录 `input`，1 家公司、4 类业务、11 个资质目录、19 个文件。
- 沿用此前测试的关闭最终送审设置；程序自身的输入生成、业务清理、上传、保存、重试和校验正常执行。
- 使用现有登录状态；正式入口按现有实现先登录 Chrome、后单 worker Chrome。本次不为减少窗口而更改程序。
- 运行前记录源码 SHA-256；运行期间不改业务代码，不附加浏览器控制、不注入监听、不刷新/点击、不调用接口补救，不重跑。
- 仅读取程序日志、退出码和调度状态。若阻塞或失败，保留事实，不把人工修复后的结果当成本次成功。

## 步骤与验收

1. 记录输入规模和源码快照。
2. 正式入口启动一次，观察原有流程。
3. 记录最终状态、停留阶段及原始错误的脱敏摘要。
4. 比较源码快照，确认无干预；按实际结果记录成功、失败或未结束。

## 依赖与范围外

依赖任务 61。无代码开发、依赖变更、打包发布、人工纠错和最终送审。

## 状态

已完成一次正式入口无干预测试，结果失败；失败发生于 URL 查询，尚未进入资质上传。程序正常按失败路径结束，退出码 1；未重跑，源码快照一致。详见 result.json、run-log.txt 和 bugs.md。

# 测试

扫描后标题消失/新增导致索引位移、隐藏同名标签、目标消失、正常新增/切换/删除业务。

原代码4项用例均复现失败：前方标题移除导致原nth(2)超时；插入标题导致误点无效标题；旧标签隐藏导致超时；目标改名后误点业务3。修复后4项通过（4.50秒）。相关新增、类型选择、删除及上传保存共14项回归通过（85.64秒）；最终定位写法使用既有版本支持的可见选择器交集，不新增依赖。

最终定位写法的6项新增/删除/类型选择复核通过（10.60秒）；静态编译和git diff --check通过。

# 问题记录

| 编号 | 问题 | 复现条件 | 状态 | 解决方案 |
| --- | --- | --- | --- | --- |
| 60-01 | 现有登录状态失效 | 专用 Chrome 打开工作台后提示人工登录 | 已恢复 | 用户人工登录，程序验证后继续 |
| 60-02 | 采集收尾在浏览器驱动关闭后移除监听抛出 TargetClosedError | 导航异常触发 BrowserSession 关闭后调用 trace.close | 已修复并回归通过 | 容忍已关闭驱动的解绑异常，仍落盘结束事件并关闭文件 |
| 60-03 | URL 列表初始化与程序查询发生交错 | 初始不同参数的查询有一次返回 1011，程序查询出现网络失败 | 已定位阶段；正式导航待修复 | flice 只是跳转入口，并非失败根因；诊断脚本在同一浏览器初始化完成后只重试查询成功 |
| 60-04 | 第三张已自动保存，程序仍等待新的保存响应并超时 | r228 已保存全部三文件，随后才进入 _save_qualification_card；页面脏检查直接返回 | 已真实复现；正式上传逻辑待修复 | 后续需从上传前监听完整保存生命周期，核对已有成功保存与远端状态，不能假设每次调用方法都会发请求 |
| 60-05 | 保存后首次进入编辑页未加载表单 | geturlinfo 返回 HTTP 200 / status 1009 | 只读重载后恢复；业务码语义待查 | 同一页重载后展示三文件及已保存待送审，哈希和举证全部通过；不能将此读取错误视为上传失败 |
| 60-06 | multipart 观测未取得文件二进制却记录 bytes=0 | Chromium 省略 post_data_buffer 中的文件内容 | 采集器标注已修正并回归 | 改为 captured_bytes 和省略状态；报告说明旧事件不代表空文件，以输入及服务端内容哈希为准 |
| 60-07 | 初期诊断结束即关闭浏览器，导致重复启动 | 未找到可恢复草稿后直接退出脚本 | 已修复 | 会话持续运行，按命令重新载入诊断动作；单会话完成上传、源码审计、异常检查与持久化复核 |

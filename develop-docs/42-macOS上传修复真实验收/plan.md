# 任务 42：macOS 上传修复真实验收

## 目标与范围
使用任务 41 最新源码、本机 Chrome 和当前 input 执行真实填写，验证逐张上传、预览回填、保存后文件数量。依赖任务 41 和当前登录状态，无新增依赖。

## 步骤
1. 沿用已通过的 214 项完整回归及 2 项补充页面测试结果。
2. 运行 main.py --no-final-submit，串行处理当前输入，跟踪脱敏日志。
3. 核对执行状态、上传和保存结果；发现失败记录原始阶段并处理。

## 验收标准
当前公司全部文件上传、自动保存和最终集合核验成功；无重复重试；最终送审关闭。浏览器失败也应正常关闭。

## 不在范围内
最终送审、发布、清理用户输入或登录状态、自动处理安全验证。

## 状态
执行中。用户已授权运行 mac 端测试，真实登录如失效须人工完成。

## 实页发现与修正
首次真实运行误报保存后缺图。只读复查确认 4 个嵌套预览容器、计数 3/4、3张图片已加载、已保存待送审。将新增检查的容器数量要求由等于1改为大于0，保留严格文件数量核验。增加本地页面回归后重新执行真实填写。

## 验收结果

2026-09-10 macOS 真实填写运行两轮。首次运行发现保存后嵌套预览容器检查过严，已修复；新增嵌套及缺失预览共3项本地 Chrome 测试通过。

第二轮 11:22:59–11:25:24：当前公司全部业务的文件上传、逐卡自动保存及保存后预览检查完成，无上传重试。最终集合检查失败：最终举证链接不一致：推广审查/资质1。因此整体真实验收未通过，不能标记公司成功；未执行最终送审，独立 Chrome 已关闭。

失败日志：run-20260910-112259-88d5eb70.log。举证链接保存/切换回读问题尚未修复。本任务未发布安装包。

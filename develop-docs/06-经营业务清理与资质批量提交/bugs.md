# 任务 6：Bugs

| ID | 发现日期 | 问题 | 复现条件 | 状态 | 解决方案 |
| --- | --- | --- | --- | --- | --- |
| BUG-007 | 2026-07-29 | 当前公司“二类医疗器械/资质1”和“资质3”使用相同资质编号但名称不同，旧规则错误阻止输入 | 执行 `python main.py` 或 `python main.py --dry-run` | 已修复 | 允许编号重复，改用“资质编号 + 资质名称”联合键完成输入校验、页面匹配、删除验证和执行历史识别；联合键完全重复时仍拒绝 |
| BUG-008 | 2026-07-29 | 旧资质和删除入口在执行阶段才检查，可能先修改前面类型后才发现后续类型不可处理 | 流程 6 存在多个已有经营业务 | 已修复 | 在任何修改前展开并扫描全部已有目标业务，校验重复编号、旧资质删除入口、编辑入口和备用资质入口；dry-run 输出完整删除/添加计划 |
| BUG-009 | 2026-07-29 | 文件预览数量选择器重叠且等待异常被忽略，上传未完成仍可能提交 | 上传一个或多个资质文件 | 已修复 | 改为逐文件监听并校验 `savelicepic` 的 HTTP、`status == 0` 和服务端文件标识，全部成功后才填写提交 |
| BUG-010 | 2026-07-29 | 编辑已有资质时点击旧文件删除按钮后没有确认清零 | 编辑包含旧文件的资质 | 已修复 | 对比卡片文件数和弹窗文件项，逐项删除并等待数量递减，未归零则禁止上传和提交 |
| BUG-011 | 2026-07-29 | OCR 可能在字段校验后延迟回写，关闭最后出现的 OCR 弹窗后直接提交 | OCR 异步返回较慢 | 已修复 | 增加字段稳定窗口；发现变化就按 input.json 重新覆盖，提交前再次稳定校验 |
| BUG-012 | 2026-07-29 | 永久资质只清日期，没有回读永久复选框状态 | `有效期至：永久` | 已修复 | 回读永久复选框，并验证日期为空或控件禁用；日期型资质同时确认永久未勾选 |
| BUG-013 | 2026-07-29 | 仅按文件数量判断内容一致，数量相同但文件不同会错误跳过 | 已有卡片与输入文件数量相同 | 已修复 | 当前改为整类覆盖，不再跳过已有资质；服务端文件标识及输入文件 SHA-256 仅用于提交后校验和审计 |
| BUG-014 | 2026-07-29 | 流程 6 页面操作没有页面级自动测试 | 运行原测试套件 | 已修复 | 增加本地 HTML + 无头 Chrome Playwright 测试，覆盖页面扫描、上传接口、OCR、永久字段、提交和旧文件清理 |
| BUG-015 | 2026-07-29 | 资质删除确认弹窗使用通用文案且不包含资质编号，程序误报确认信息不一致 | 删除已有可删除资质 | 已修复 | 删除目标仍在经营业务卡片内按编号和名称精确定位；确认弹窗允许明确包含“删除/移除资质”意图的通用文案 |
| BUG-016 | 2026-07-29 | 可删除卡与不可删除首卡的编号和名称相同，删除成功后因首卡仍存在而误报失败 | 删除与首卡同标识的备用资质 | 已修复 | 删除前后同时比较同标识卡数量和已填卡总数，均准确减少 1 才判定成功，不再要求同标识卡完全消失 |
| BUG-017 | 2026-07-29 | 单个资质提交后立即扫描异步卡片，误报未找到或文件标识不一致，并在页面未稳定时开始下一条 | 连续新增多条资质 | 已修复 | 上传接口继续校验服务端文件 ID；页面提交后等待字段和文件数量连续稳定，再开始下一条。单项失败立即输出原始错误并停止当前公司，不再由最终集合错误掩盖 |
| BUG-018 | 2026-07-29 | 新增经营业务后第一张必填资质直接展开为面板内嵌表单，程序仍等待弹窗而超时 | 新增“一类医疗器械”等经营业务后填写首张资质 | 已修复 | 空白首卡已包含完整上传控件、字段和提交按钮时，直接将卡片作为表单根节点处理；普通备用资质继续使用弹窗 |

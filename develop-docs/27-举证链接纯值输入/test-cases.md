# 测试场景

- 正常：纯 HTTPS 链接，含端口、查询参数的链接正确解析和持久化。
- 边界：空文件、仅空白、UTF-8 BOM、带空白与单斜杠协议的链接。
- 回归：旧四字段、仅举证链接标签、错误行与标签混用、重复标签校验。
- 回归：多个资质目录仅链接，无编号、名称时不触发重复检测。
- 回归：缺少表单文件仍报错；现有文件数量限制和应用测试通过。

执行结果：`.venv/bin/pytest tests/test_input_loader.py tests/test_application.py -q`，24 项通过。

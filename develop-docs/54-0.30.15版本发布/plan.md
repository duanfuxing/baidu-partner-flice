# 0.30.15版本发布

目标：发布任务52、53的上传滚动和逐文件调用页面保存方法修复。
范围：同步版本、提交并推送master与v0.30.15标签，触发四平台打包及自动Release。
步骤：确认标签未占用；版本资源检查；提交和推送版本标签。
依赖：现有GitHub Actions/PyInstaller，无新增依赖。
验收：版本一致、提交推送成功且版本标签触发打包。用户明确要求不监听打包结果，因此不等待CI或验证安装包附件。
不在范围内：真实业务操作、最终送审、安装包下载验收。
状态：完成提交与触发。代码提交5e76047，master及v0.30.15标签已原子推送成功；标签推送触发既有四平台打包和自动Release流程。按用户要求未监听构建结果，不声明安装包已构建成功。相关业务回归见任务53。

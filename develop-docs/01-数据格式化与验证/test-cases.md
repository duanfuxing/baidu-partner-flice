# 任务 1：测试用例

| 编号 | 场景 | 预期结果 |
| --- | --- | --- |
| T1-01 | 当前示例目录 | 全量解析成功 |
| T1-02 | `url.txt` 包含首尾空格和换行 | 输出 URL 为清理后的字符串 |
| T1-03 | `url.txt` 清理后为空 | 报错并指出公司目录 |
| T1-04 | 缺少 `表单信息.txt` | 报错并指出资质目录 |
| T1-05 | 缺少资质编号、名称或有效期 | 报错并指出字段名 |
| T1-06 | 有效期为 `永久` | `permanent=true`，日期为空 |
| T1-07 | 有效期为合法日期 | 输出标准日期 |
| T1-08 | 有效期为非法日期 | 报错 |
| T1-09 | 举证链接为空 | 解析成功，字段为空 |
| T1-10 | 目录包含 txt、docx 等非支持文件 | 忽略非支持文件 |
| T1-11 | 文件数量超过 9 个 | 报错 |
| T1-12 | 单个文件超过 10MB | 报错 |
| T1-13 | 全部输入校验通过 | 每个公司目录生成 `input.json` |
| T1-14 | 任意公司校验失败 | 不生成或更新任何 `input.json` |
| T1-15 | 读取生成的 `input.json` | 中文正常显示，文件路径相对公司目录 |
| T1-16 | URL 使用 `http:/` 或 `https:/` | 自动修正为标准双斜杠协议 |

# 测试场景

- 首次移出无请求，第二次触发保存成功。
- 已发出请求但响应慢：只提交一次。
- 三次均未发请求：明确失败。
- HTTP200业务失败：不标记保存成功。
- 填写时立即保存：监听不遗漏。
- 长页面及内部滚动容器到达底部。
- 原有上传、自动保存与后台焦点回归。

结果：本地无头Chrome组合回归11 passed、55 deselected；最终新增场景8 passed，包含首次事件丢失恢复、响应不返回不重提、延迟响应成功仅一请求、三次无请求失败、业务错误、填写时立即保存、双层滚动和超长卡片。两组包含重合用例，不累计为19个独立测试。静态编译及diff检查通过。初次沙箱Chrome启动受限，获得权限后重跑通过。真实Windows页面仍待验收。

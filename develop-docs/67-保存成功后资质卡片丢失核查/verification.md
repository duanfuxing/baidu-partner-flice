# 复现、修复和验证

## 本次用户失败的实际含义
用户20:14:51运行在推广审查资质3首图保存后失败。上传、submitlice、geturlinfo均HTTP200/status0，随后DOM里第三张卡未恢复。只读诊断重新打开后实际显示3、3、1个文件，证明第7个文件已持久化。

页面代码会把详情响应转成补充资质集合并整体替换表单。旧实现只处理GET失败重读，没有处理GET成功但卡片未恢复；因此一直等待后报错。原失败没有保存详情响应内容，无法进一步确定当时是响应数据暂时不完整还是前端渲染异常，未将推测写成结论。

## 改动
- 卡片持续缺失2秒、详情读取结束、本次保存确认成功且没有写入在途时，核对本次已观察到的同公司/URL详情请求。
- 文件集合、身份、举证唯一匹配后，调用已核查的页面BusinessFormCard.onInit，只读刷新业务一次。适配真实Vue Fragment根节点。
- 刷新后继续严格校验卡片、文件ID、顺序、内容和举证；读回不符、写入仍在途、无唯一回调、刷新仍不正确时不会重传或重复保存。
- 首次创建请求中的lice_index/licemap_id为0时，按未分配ID处理，仍要求其余身份及完整文件唯一一致。

## 本地验证
18项保存后稳定性回归、30项全程保存观察回归通过。新增7项卡片丢失恢复/拒绝用例及1项从文件上传开始的完整链路恢复用例通过，共56个相关场景。完整链路故意模拟保存成功、首次详情成功但未包含新卡，最终只上传和保存一次，自动只读恢复后继续验证。

早期新增夹具缺少真实业务标题，删除卡片后容器高度为0导致3项预期失败；补齐标题后通过。未为夹具放宽可见性保护。

## 实际页面验证
同一项目专用只读诊断会话验证原生业务刷新，前后3、3、1文件集合一致，0上传/保存/送审请求，正常关闭。采集目录 `logs/qualification-trace-20260912-204143/`。

随后正式入口完整运行20:51:32–20:58:18，退出码0。4类业务、11份资质、19文件全部核验通过；复用7文件，上传12文件，未发起最终审核。公司状态success，待处理和运行中队列均空。运行前后main.py及src哈希一致，运行中无人工操作。见 [脱敏日志](round-1.log) 和 [结果摘要](round-1-result.json)。

真实完整运行没有再次出现丢卡，因此不能称本轮真实触发了恢复分支；恢复分支由本地故障链路和真实只读回调验证支撑。没有清空用户已保存文件来强制制造一次从零上传。

## 用户运行起点已确认
用户确认手动运行前删除或清空过之前的资质，因此此次走新增推广审查符合预期，续传入口没有误判。原失败发生在从空数据新增时。本次修复后真实全量运行从失败后已保存的7文件续传，本地完整上传用例覆盖空卡保存后丢失及自动恢复。

未打包或发布，手动运行命令保持不变。

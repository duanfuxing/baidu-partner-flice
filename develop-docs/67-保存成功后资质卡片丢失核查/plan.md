# 保存成功后资质卡片丢失核查

## 目标与证据
用户20:14:51完整运行，在推广审查资质3首文件保存后失败。20:17:32 submitlice HTTP200/status0，20:17:33 geturlinfo HTTP200/status0，但当前卡片在30秒内未重现。此次不是非零读取响应，不能靠任务65的网络错误重读解决。

## 步骤与设计
- 保留用户失败日志，调查为何从已保存续传转为新增、成功详情响应中的实际集合与DOM的关系。
- 复用一个项目专用诊断Chrome，只读检查已有数据、页面组件与刷新行为，禁止人工修补保存内容。
- 根据证据修复；不将接口成功当作完整保存，不重复写入未知结果。
- 本地Chrome重现具体故障并验证恢复及拒绝边界，随后正式入口全量实测；运行中仅观察。

## 依赖与范围
依赖61–66，不修改用户输入或删除远端资质，不最终送审，不发布。不为通过测试盲目延长等待。

## 验收
解释本次HTTP成功仍丢卡原因；修复后本地故障场景通过；完整实测记录真实结果及限制。

## 状态
修复、相关回归及真实完整运行完成。原失败的响应内容缺失，精确的服务端/前端原因边界见验证记录。

## 只读调查与实现方案
诊断一次Chrome会话确认重新打开后资质文件数为3、3、1，第7文件已持久化；无写入。静态AddInvestLice刷新函数以详情响应重建reserveLice_items，AddReserveLice深度watch会替换补充卡集合。用户失败日志没有响应内容，不能断言当时是服务端旧快照还是前端渲染故障。

新增恢复：仅卡片持续缺失、详情读取已结束且保存观察器确认本次成功，无任何进行中写入时，先按已观察同公司URL的详情GET严格核对本卡文件/身份/举证；通过后调用唯一BusinessFormCard组件的页面onInit读取回填一次。刷新后保留所有文件、内容、顺序和举证校验；不存在唯一回调、读回不符、存在写入或刷新失败均不重传保存。仅接受页面实际观察到的组件结构与回调，不构造参数、不修改Vue状态。

最终完整实测通过，验证范围和未确定信息见 [验证记录](verification.md)。

# 测试记录

用户真实复现：保存和详情读取成功，资质3首文件保存后DOM卡片消失，30秒超时。待调查与回归。

新增7项恢复/拒绝场景通过，1项完整单资质上传丢卡恢复通过。首版夹具删除唯一卡后业务容器高度为0，与真实仍保留标题的结构不同；补齐业务标题后通过，未放宽可见性保护。真实Vue组件Fragment根适配后，同一只读诊断Chrome原生刷新成功、3/3/1文件保留，0上传/保存/送审请求，已正常关闭。旧回归批次尚在收尾。

原保存稳定/全程观察回归批次已结束，所有原用例通过；该早期批次的新增3项失败均为补齐业务标题前的夹具，修正后7项专测及完整上传1项均通过。

最终完整实测通过，验证范围和未确定信息见 [验证记录](verification.md)。

# 流程 1–6 开发任务

## 目标

完成从 `input` 目录读取资质数据，到进入指定公司的 URL 资质提交区域，按经营业务类型批量维护资质并完成页面级最终提交的自动化流程。

## 任务顺序

| 顺序 | 任务目录 | 目标 |
| --- | --- | --- |
| 1 | [01-数据格式化与验证](./01-数据格式化与验证/) | 已实现；当前输入有格式问题待处理 |
| 2 | [02-浏览器登录与会话](./02-浏览器登录与会话/) | 已实现；需要本机 GUI Chrome 手工验收 |
| 3 | [03-公司搜索](./03-公司搜索/) | 已实现；需要登录后联调接口 |
| 4 | [04-进入资质页面](./04-进入资质页面/) | 已实现；需要真实页面验收 |
| 5 | [05-URL匹配与资质区域](./05-URL匹配与资质区域/) | 已实现；需要真实页面验收 |
| 6 | [06-经营业务清理与资质批量提交](./06-经营业务清理与资质批量提交/) | 已实现并完成真实页面联调 |
| 7 | [07-本模块最终提交与自动关闭](./07-本模块最终提交与自动关闭/) | 已实现；成功后最终送审并自动关闭 Chrome |
| 8 | [08-多公司双Worker调度](./08-多公司双Worker调度/) | 历史实现；双 worker 策略已被任务 9 的单 worker 策略替代 |
| 9 | [09-桌面应用与跨平台打包](./09-桌面应用与跨平台打包/) | 已实现；桌面交互、单 worker、运行日志和 Windows/macOS 打包 |
| 10 | [10-桌面应用代码审查修复](./10-桌面应用代码审查修复/) | 已完成；Windows PID、登录验证、结果语义、日志性能、取消和打包自检 |
| 11 | [11-桌面UI视觉重构](./11-桌面UI视觉重构/) | 已完成；现代浅色主题、页面导航、统计卡、状态反馈和独立日志页 |
| 12 | [12-最终提交开关与任务选项精简](./12-最终提交开关与任务选项精简/) | 已完成；移除桌面 dry-run/截图选项，增加默认关闭的最终提交控制 |

## 依赖关系

```text
任务 1 ──┬──> 任务 3 ──> 任务 4 ──> 任务 5 ──> 任务 6
         └──> 任务 2 ────────────────┴───────┘
```

任务 3–6 依赖任务 2 提供已登录的浏览器上下文。任务 3 产生的 `custId` 是任务 4 的输入，任务 4 产生的目标页面是任务 5 的输入，任务 5 定位到“行业资质”区域后由任务 6 清理经营业务并逐项提交资质。

## 每个任务目录

- `plan.md`：开发范围、设计、步骤、验收标准和风险。
- `test-cases.md`：测试场景和预期结果。
- `bugs.md`：问题记录。没有问题时保留空模板，不删除。

## 当前约定

- 业务代码位于根目录 `src/`，根目录入口为 `main.py`。
- 执行 `python main.py` 默认读取 `input/`；也可以使用 `python main.py --input <目录>` 指定输入目录。
- 依赖统一记录在根目录 `requirements.txt`，项目元数据同步维护在 `pyproject.toml`。
- 流程 1 校验通过后，在每个公司目录写入标准化的 `input.json`，后续流程优先读取该文件。
- 使用 Python + Playwright 控制本机安装的 Chrome。
- 首次无登录状态时暂停，提示人工登录；登录成功后保存 Cookie 和本地存储信息。
- URL 只清理空白字符后进行精确匹配。
- 资质页面成功标志为 URL 的 Hash 以 `#/truth/submit` 开头，并且页面出现“URL状态概览”；允许 HTTP/HTTPS 和动态查询参数。
- 删除经营业务前必须完成输入类型到页面类型的精确映射预检；映射不明确时禁止删除。
- 页面默认缺少输入类型时，通过“经营业务”下拉框精确搜索并添加；不得用近似名称代替。
- 每个保留业务按 `input.json` 整类覆盖：删除所有可删除旧资质，用 JSON 第一条编辑不可删除首卡，其余全部重新上传。
- 上传文件后如出现 OCR 提示则关闭提示，并以 `input.json` 中的字段重新填写和校验表单。
- 所有单项资质及最终集合验证成功后，根据桌面任务选项决定是否点击“本模块提交/全部提交”；启用时必须校验 `submitall`。
- 主登录 Chrome 和每个 worker Chrome 均在对应阶段结束后自动关闭，失败信息保存在状态 JSON。
- 主进程只执行一次登录流程，关闭登录 Chrome 后自动使用 1 个独立 worker 串行处理公司。
- 调度状态统一写入 `input/scheduler.json`，每家公司同时维护 `execution-status.json`。
- 流程错误默认记录后跳过当前公司，继续处理后续公司；不可恢复的配置错误可以终止任务。

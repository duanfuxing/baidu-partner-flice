# 验证记录

| 场景 | 预期 | 结果 |
| --- | --- | --- |
| 初始重复 GET / 参数初始化变化 | 等待旧请求结束，重新点击一次，只接受新结果 | 通过（本地） |
| 请求乱序完成 | 全部结束前不重查，不接受旧结果 | 通过（本地） |
| 查询持续竞争 / 请求一直未结束 | 有界失败，不点击详情 | 通过（本地） |
| 单次 HTTP/业务错误 / 未授权 | 保留原失败语义 | 通过（本地） |
| 回填期间新请求 | 不误用旧表格，按竞争处理 | 通过（本地） |
| 正式完整运行 | 无人工干预完成全部资质，记录真实结果 | 通过，见任务66 |

本地 URL 查询原37项通过；新增竞争7项通过，共44项。新夹具首次缺少链接 href 导致4项表格匹配失败，补全真实链接结构后7项通过，未为适配夹具放宽生产表格校验。

初始入口重试6项及新版弹窗入口回归1项通过。原 URL 测试新增总数13项，URL模块合计50场景已分别验证。

第二、四轮真实运行均检测到查询竞争、等待旧请求结束后重新查询并正确进入详情；入口缺失首次重试分支由本地验证。全量结果见任务66。

最终验收：任务66第五轮全量成功，4类业务11份资质19文件核验通过，未送审。测试全程无人工干预。见 [完整验证](../66-空业务页新增卡片兼容/verification.md)。

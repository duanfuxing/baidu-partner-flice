# 回归场景

缺失后补传成功、连续缺失两次停止、空卡首图缺失恢复、旧回调迟到、额外条目、原ID冲突、原有失败项删除重传。

测试使用本地HTML和模拟上传响应：第一轮成功但不添加预览，第二轮正常添加；连续两轮都不添加只发送两次；首张文件缺失时基线为空也可恢复；缺失响应与原图ID冲突则不补传；补传前出现迟到条目不发第二次请求；第二轮同时回填两项时停止且不删除原有图片。兼容既有404、5xx、业务错误、重试上限和异步误删保护。

结果：恢复18项、正常上传保存和旧有重试14项，共32项相关回归通过。未执行本次Windows真实页面验收。

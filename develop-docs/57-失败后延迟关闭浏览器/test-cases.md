# 测试场景

- 可见页面发生业务错误：等待5秒再关闭；原错误继续上抛。
- 正常成功：立即关闭，不等待。
- 启动失败无窗口、上下文无页面、用户已关闭页面、浏览器断开或无头模式：不等待。
- 等待或可见性检查失败不覆盖原业务错误，仍执行关闭。
- 浏览器与worker现有回归。

结果：8项用例通过（包含在任务56的70项联合回归中）。等待通过mock验证准确秒数和关闭顺序，覆盖错误、成功、无头、无浏览器、断开连接、无页面、页面关闭和等待异常；原异常保持不变。Windows可见窗口人工验收待执行。

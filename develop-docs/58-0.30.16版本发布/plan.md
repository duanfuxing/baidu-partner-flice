# 0.30.16版本发布

目标：按用户要求提交本次修复，更新版本并触发跨平台打包。
范围：任务55保存后稳定判断、任务56 URL接口监听、任务57错误后延迟关闭；版本统一为0.30.16，提交master及v0.30.16标签。
设计：沿用现有标签触发的GitHub Actions，四平台测试、打包及自动Release；动态认证参数和用户输入不进入提交。
步骤：检查工作区和远端版本；同步程序/安装器/资源/工作流及测试版本；运行版本相关检查；提交并原子推送master和标签；确认打包已触发。
依赖：任务55–57及现有GitHub Actions，无新增依赖。
验收：版本一致、检查通过、代码和标签已推送、打包工作流已触发。
不在范围：等待打包完成、下载或安装验收、真实公司操作。沿用此前只触发、不等待打包结果的发布约定。
状态：完成提交和打包触发。代码提交83d5270，master与v0.30.16已原子推送。GitHub Actions运行34586821089由标签push触发，headSha与代码提交一致，查询时为queued。按发布范围不等待最终构建结果，不声明安装包已生成。
构建链接：https://github.com/duanfuxing/baidu-partner-flice/actions/runs/34586821089

# 任务 28：表单信息文件可选

## 目标与范围

资质目录不包含 `表单信息.txt` 时，视为不提供举证链接，不再阻止输入校验。

## 设计与实现步骤

1. 在任务 27 基础上，缺少表单文件时使用空字段数据；已有文件继续按原规则解析。
2. 将缺失文件报错测试改为成功解析及 JSON 空链接持久化测试。
3. 更新需求说明并运行输入解析和应用回归测试。

## 依赖

依赖任务 27；无新增依赖。

## 验收标准

- 缺少文件、空文件均解析为 evidenceUrl=null。
- 已有纯链接和旧标签格式仍可解析。
- 缺少实际资质图片/PDF 仍报错，文件数量和大小限制保持有效。

## 不在范围内

不修改旧版页面必填预检、上传或送审逻辑，不改写用户资料。

## 验收结果

已完成。输入解析和应用回归测试共 24 项通过；缺少表单文件可持久化为空链接，缺少图片/PDF 仍被拦截。未运行真实浏览器操作。

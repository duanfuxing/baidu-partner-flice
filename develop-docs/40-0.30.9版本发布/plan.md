# 任务 40：0.30.9 版本发布

## 目标与范围

提交任务 33–39 的超时设置、固定 macOS 安装目录、步骤日志与上传重试、界面布局、独立设置菜单、历史日志删除及验收记录，以 0.30.9 发布。

## 设计与步骤

确认标签未占用；同步应用和四架构安装包版本；执行版本一致性测试；提交代码并推送 master 与 v0.30.9 标签；确认标签 Actions 已触发。

## 依赖

任务 39 完整自动化测试及真实填写验收通过；使用现有 GitHub Actions，无新增依赖。

## 验收标准

版本统一为 0.30.9；安装器测试通过；发布提交和标签推送成功；四架构打包已触发。

## 不在范围内

不覆盖旧版标签，不提交 input、认证状态、运行日志，不执行最终送审。

## 验收结果

版本已统一为 0.30.9；安装器及打包资源 12 项测试通过，git diff --check 通过。任务 39 完整 pytest 与真实填写通过。准备推送标签；云端构建与新版系统安装结果待后续确认。

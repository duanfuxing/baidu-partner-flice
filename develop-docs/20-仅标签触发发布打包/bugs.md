# 问题记录

## BUG-001：同一提交触发分支和标签两次打包

- 状态：已修复
- 复现条件：先推送 `master`，随后为同一提交推送 `v*` 标签。
- 现象：GitHub Actions 同时出现分支构建和标签构建，重复消耗四平台构建资源。
- 原因：工作流的 `push` 同时监听 `master/main` 分支和 `v*` 标签。
- 解决方案：删除分支推送触发条件，仅保留版本标签触发发布打包。

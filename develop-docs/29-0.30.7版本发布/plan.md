# 任务 29：0.30.7 版本发布

## 目标与范围

将最新代码以 0.30.7 发布，统一应用、Windows/macOS 安装包和工作流版本。

## 设计与步骤

1. 确认远程版本标签未占用。
2. 同步源码、项目元数据、打包配置、版本测试和 README。
3. 运行版本一致性和输入回归测试。
4. 提交并推送 master，创建 v0.30.7 标签触发四架构构建和 Release。

## 依赖

依赖任务 27、28；使用现有 GitHub Actions，无新依赖。

## 验收标准

版本全部为 0.30.7；定向测试通过；远程标签指向本次提交；标签构建已启动。

## 不在范围内

不修改业务流程，不覆盖历史标签或旧 Release。

## 验收结果

版本一致性、安装包配置、输入和应用回归共 34 项测试通过。准备推送版本标签；云端构建结果和安装验收需后续确认。

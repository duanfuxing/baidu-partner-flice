# 任务 24：测试用例

| 编号 | 场景 | 预期结果 |
| --- | --- | --- |
| T24-01 | 解析当前 input | 返回 1 家公司和全部资质类型，不修改原始资质文件 |
| T24-02 | 完整 pytest | 全部测试通过 |
| T24-03 | CLI dry-run | 登录、公司搜索、资质入口和 URL 详情定位成功，不修改资质页面 |
| T24-04 | 新版页面默认业务1–4 | 保留推广审查，安全删除业务2–4 |
| T24-05 | 多类型、多资质目录、多文件真实填写 | 各类型独立业务卡；每个资质目录独立卡片，目录内 1–9 个文件上传到同一卡片并成功触发 `submitlice` |
| T24-06 | 一个资质目录的文件及举证链接填写完成 | 点击卡片外安全空白处，20 秒内捕获一次 `submitlice`，校验 HTTP 200、`status == 0` 且举证链接回读一致 |
| T24-07 | 最终提交选项关闭 | 不点击“发起审核”，不触发 `submitall` |
| T24-08 | 页面结构或权限不符 | 停止当前公司并记录明确错误，不继续破坏性操作 |
| T24-09 | CLI 使用 `--no-final-submit` | 执行真实填写和自动保存，但 `WorkflowConfig.final_submit` 为 false，不调用 `submitall` |
| T24-10 | 详情页先显示骨架屏，随后异步出现“新增资质” | 等待唯一按钮加载后再点击，不因首屏骨架误报缺失 |
| T24-11 | 默认业务只有1–3且不含推广审查 | 先精确新增推广审查，再从后向前删除原有全部默认业务 |
| T24-12 | 默认业务数量变化但含唯一推广审查 | 保留推广审查，删除所有其他默认业务，不依赖固定序号上限 |
| T24-13 | 真实结构 business-form-card 与顶部同名标签并存，旁边卡含隐藏日期 combobox | 只选目标业务卡和它的查询框；已通过页面夹具测试 |
| T24-14 | 非百度域名呈现同名新增按钮 | 新增页识别返回 false；已通过页面夹具测试 |
| T24-15 | 精确经营业务候选 aria-disabled=true | 立即报告候选被百度禁用，不点击，不继续删除或上传 |
| T24-16 | 同类型存在多个资质目录 | 第一目录使用自带卡片，第二目录勾选“我还需要补充其他资质”，第三目录起逐次点击“新增补充资质”；一个目录内多文件不创建额外卡片 |
| T24-20 | 单个资质目录包含 1–9 个文件 | 一次选择该目录全部文件，逐个校验 `savelicepic` 响应，全部回填后只在当前卡片触发自动保存 |
| T24-21 | `savelicepic` 响应先返回、上传组件 success/`uploaded` 回调延迟 | 等待卡片预览计数达到目录文件数后才移出卡片；不得在 Vue 的 `formData.filename` 尚未回填时触发表单校验 |
| T24-17 | 直接设置隐藏文件输入不会自然触发卡片 mouseleave | 文件全部上传并填写举证链接后点击卡片外安全空白处，只等待本资质卡片最终一次 submitlice |
| T24-18 | 卡片自动保存响应延迟 | 点击空白处后最多等待20秒；超时则停止当前公司，不继续创建下一资质卡片 |
| T24-19 | 上次失败留下唯一“已保存待送审”的推广审查 | 从已备案业务资质匹配行点击“修改”恢复，不点击新增资质，不重复新增推广审查 |
| T24-22 | 补充资质保存刷新后复选框恢复为未勾选 | 识别已有补充卡，重新勾选后等待“新增补充资质”恢复，再创建下一卡 |
| T24-23 | 新增业务选型后主资质配置延迟渲染 | 0 个文件控件时只等待内置主卡，不点击任何补充入口 |
| T24-24 | 经营业务自动生成2张内置必填卡，输入含3个资质目录 | 前2个目录依次填入两张内置卡，第3个目录创建1张补充卡；最终3卡全部保存 |
| T24-25 | 双击最新 macOS `.app` | 显示桌面 GUI，无终端窗口；可选择输入目录、校验并启动任务，“完成后执行全部提交”默认未勾选 |

## 2026-09-06 续调记录

- 修复前完整 124 项测试通过，`git diff --check` 通过。
- 只读真实 DOM 诊断确认默认有 3 项业务、均未匹配推广审查；未删除、上传或送审。
- 真实业务卡为 `.business-form-card`，空白业务查询框为无 placeholder 的 `input.el-select__input[role=combobox]`。
- 卡内另含隐藏的日期 combobox；不能将通用查询控件存在当作经营业务输入框可见的依据。
- 新增页来源校验、真实卡片边界和异步入口共 3 项针对性测试通过。
- 本轮修复后完整 126 项测试通过，`git diff --check` 通过。
- 多文件自动保存及最终集合真实验收仍未完成，不得将夹具通过等同于真实流程 6 完成。
- 真实 `--no-final-submit` 重跑停在推广审查候选禁用；调度结果 failed、finalSubmit=false。未删除原有默认业务、未上传文件，未触发 submitall。搜索存在不等于允许选择。
- 禁用检测修复后，新版页面及新版资质单元测试回归通过；`git diff --check` 通过。126 项全量通过记录发生于新增禁用检测用例之前。
- 2026-09-08：后续真实验收样本切换为 `input/东莞山虞医药有限公司`；武汉样本特殊状态不作为通用流程验收依据。禁用候选的安全停止逻辑保留为防护。
- 2026-09-09：东莞样本真实 `--no-final-submit` 完整通过；4 个业务类型、11 个资质目录全部完成 `submitlice` 自动保存，调度结果 1 成功、0 失败，未触发 `submitall`。
- 2026-09-09：完整 142 项测试通过，`git diff --check` 通过；新增覆盖补充开关恢复、主卡异步渲染和“2张内置卡+1张补充卡”的浏览器夹具。
- 2026-09-09：使用当前源码重新构建 `dist/百度资质自动提交工具.app`；确认版本 0.30.6、Mach-O x86_64、windowed 进程可通过双击等价方式启动，未产生崩溃报告；启动时未选择输入目录、未执行任务。

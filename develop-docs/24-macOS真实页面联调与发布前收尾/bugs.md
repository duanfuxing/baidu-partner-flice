# 任务 24：问题记录

| 编号 | 日期 | 问题 | 复现条件 | 状态 | 解决方案 |
| --- | --- | --- | --- | --- | --- |
| BUG-024-01 | 2026-09-06 | CLI 只有 dry-run 和默认最终提交，缺少“真实填写但不最终送审”的安全模式 | macOS 真实流程 6 联调 | 已修复 | 增加 `--no-final-submit` 并验证配置透传 |
| BUG-024-02 | 2026-09-06 | 新版详情页骨架屏阶段立即查找“新增资质”，且“+ 新增业务资质”的路由/文本定位不稳定 | 已验证的新版详情页异步加载并进入新增页 | 已修复 | 等待唯一“新增资质”；随后按 button 语义定位并允许可选半角/全角加号，不依赖动态 URL、面包屑或父子文本节点 |
| BUG-024-03 | 2026-09-06 | 真实页面默认业务数量和类型由 MEG 规则决定，并非固定业务1–4或固定含推广审查 | 当前公司默认出现入驻商城、三类医疗器械和电商代运营，共3项且均可删除 | 已修复，真实流程通过 | 动态扫描默认业务；若无推广审查先精确新增，再从后向前删除所有非推广审查默认业务，最终只保留唯一推广审查 |
| BUG-024-04 | 2026-09-06 | 页面根容器包含其他业务的 query/补充控件，可能导致新增业务找不到下拉框或跨业务误判类型 | 多个业务卡同时渲染，新增业务4尚未选择类型 | 已修复 | 先按候选容器包含的业务标题数量隔离当前卡片，再在卡内按 query/补充资质特征选择最小容器；统一支持 combobox |
| BUG-024-05 | 2026-09-06 | 真实详情页数据偶发超过默认30秒仍处于骨架屏 | URL信息和投放资质均未完成异步加载 | 已修复 | 仅将进入新增页前的“新增资质”安全等待延长到页面超时的2倍，超时后仍停止，不执行页面修改 |
| BUG-024-06 | 2026-09-06 | 通用祖先遍历耗时且未优先使用真实业务卡边界；新增页识别缺少域名约束 | 真实 DOM 存在 business-form-card，卡内还包含隐藏日期 combobox | 已修复，夹具及真实定位通过 | 优先使用带精确业务标题的已验证卡片容器；恢复域名、协议和 newaudit 路径检查，保留动态路由 |
| BUG-024-07 | 2026-09-06 | 查询存在精确推广审查选项，但 aria-disabled=true，点击等待超时 | 当前公司无默认推广审查，新增业务搜索该类型 | 禁用检测已修复并通过回归；页面规则待用户确认 | 点击前验证候选项 is_enabled，禁用时明确报错，禁止强制点击或替代映射；本次未删除默认业务、未上传、未送审 |
| BUG-024-08 | 2026-09-08 | 正常样本默认生成业务1–5，业务1为推广审查；删除逻辑仅从顶部标签查找关闭入口，删除业务5失败 | 东莞山虞医药有限公司真实填写 | 已修复，真实流程通过 | 真实 DOM 已确认业务2–5各自卡片内有唯一“删除”按钮；优先在精确业务卡内删除，顶部标签关闭入口仅作兼容回退。业务1已正确识别为推广审查，没有误新增 |
| BUG-024-09 | 2026-09-08 | 上传前一次性创建全部补充资质表单；百度要求前一张文件上传保存后才允许继续新增，导致“补充资质2”无法创建 | 推广审查包含9个文件 | 已修复，真实流程通过 | 改为“创建当前表单→上传并校验 submitlice→填写举证链接并校验 submitlice→再创建下一表单”的逐张流程 |
| BUG-024-10 | 2026-09-08 | 隐藏 file input 上传后页面已显示缩略图，但仍为待保存且未触发 submitlice | 推广审查第一张文件真实上传 | 已修复，真实流程通过 | 按页面提示在文件选择后进入并移出当前资质卡片，触发自动保存；分别报告文件和举证链接保存超时 |
| BUG-024-11 | 2026-09-08 | 文件已保存为“已保存待送审”，但仅让举证输入框键盘失焦未触发 submitlice，页面刷新后值为空 | 推广审查第一张文件的举证链接 | 已修复，真实流程通过 | 点击并填写举证输入框后显式将鼠标移出对应资质卡片，再等待 submitlice 和回读值 |
| BUG-024-12 | 2026-09-08 | 失败前已有单文件通过 submitlice 持久化，重跑时推广审查不再出现在默认业务且候选禁用 | 东莞正常样本在文件保存后、举证保存前失败，再次运行 | 修复中 | 取消测试表单后确认详情页出现唯一“已保存待送审”的推广审查及“修改”入口；重跑优先进入该行修改并覆盖上传，再继续其余类型，不重复新增 |
| BUG-024-13 | 2026-09-08 | 详情页刚出现“投放资质”时标签尚未异步渲染；切到业务资质后又会短暂显示0条再填充1条，恢复检测过早返回 | 已存在推广审查待送审记录的真实重跑 | 修复中 | 在决定新增前等待业务资质标签，并跳过切换后的0条过渡帧，再匹配恢复行 |
| BUG-024-14 | 2026-09-08 | 修改页视觉标题为“业务:推广审查”，但组件拆分后无法作为单一精确文本节点定位 | 点击真实“修改”后已进入编辑投放资质页 | 修复中 | 使用受信域名、编辑投放资质面包屑、文件控件和取消按钮联合确认编辑页 |
| BUG-024-15 | 2026-09-08 | 恢复编辑时重新上传第一张会追加到主卡已有图片，形成2/4，不是覆盖 | 推广审查已有一张保存文件后重跑 | 调查中 | 先定位真实图片删除与补充卡删除控件，清空本轮测试草稿的文件表单后从输入完整覆盖；禁止把追加结果当作覆盖成功 |
| BUG-024-16 | 2026-09-08 | 详情页业务记录已显示，但URL信息仍为骨架屏且操作列未渲染，新增/修改入口均缺失 | 多次 submitlice 联调后的页面重载 | 调查中 | 作为可恢复异步加载失败进行一次有限刷新；继续缺少操作入口则停止，不循环刷新或强制点击 |
| BUG-024-17 | 2026-09-08 | 举证输入后只按Tab及移动到坐标(0,0)，真实页面仍未触发自动保存 | 干净流程推广审查第一张举证链接 | 已修复，真实流程通过 | 填写后点击资质卡外安全空白区域，明确触发 change、blur 和 mouseleave，再等待 submitlice |
| BUG-024-18 | 2026-09-08 | 多次失败重跑在推广审查主卡追加了3张相同测试图片 | 东莞样本联调草稿 | 已清理 | 通过已备案业务资质列表精确选择当前输入类型的测试草稿并批量删除；全程阻断 submitall |
| BUG-024-19 | 2026-09-08 | 编辑页某次加载没有可见“URL信息”，清理图片后无法用该节点触发保存 | 测试重复图片清理 | 已修复 | 自动保存改为在当前业务卡内计算资质卡外的非交互安全空白点，不依赖固定标题节点 |
| BUG-024-20 | 2026-09-08 | 点击“URL信息/信息资质管理”后首张举证链接仍未触发 `submitlice`，且失败重跑把同一测试图片累计到主卡4张 | 东莞样本干净重跑，文件保存成功后填写首张举证链接 | 已修复，真实首张文件及举证保存通过 | source map 与 DOM 确认 `mouseleave` 绑定在外层 `div.form-card`，不是内部 `form.lice-form`；定位改为最近的 `form-card`，测试草稿通过业务列表“批量删除”清理且阻断 `submitall` |
| BUG-024-21 | 2026-09-08 | 第一张保存通过后，第二张补充资质文件偶发在上传完成前就移出卡片，此时 `filename` 尚未回填，组件不会调用 `submitlice` | 推广审查第二张文件真实上传 | 已修复，真实流程通过 | 先等待并校验 `savelicepic` HTTP 200 且 `status == 0`，确认上传组件已回填文件，再触发外层 `form-card` 的 `mouseleave` 并等待 `submitlice`；已补充浏览器夹具回归 |
| BUG-024-22 | 2026-09-08 | 新版上传层级及保存时序理解错误：把同一资质目录内多张图片拆成多个补充卡片，并分别等待文件和举证两次 `submitlice` | 推广审查资质1包含3张图，程序为第二张创建“补充资质1” | 已修复，真实流程通过 | 改为“资质目录=资质卡片”；同目录1–9张文件上传到同一卡片，填写一次举证链接并点击外部安全空白处，最长20秒等待一次 `submitlice`；只有同类型第二个资质目录才创建补充卡片 |
| BUG-024-24 | 2026-09-09 | 已保存补充资质触发父组件刷新后，“我还需要补充其他资质”可能恢复为未勾选，导致已有补充卡仍在但“新增补充资质”入口消失 | 同一业务包含第3个及后续资质目录 | 已修复，真实流程通过 | 创建后续补充卡前读取复选框真实状态；若未勾选则安全恢复勾选并等待入口重新出现，再新增下一张卡；同时补充业务类型和资质序号错误上下文 |
| BUG-024-25 | 2026-09-09 | 新增经营业务并选择类型后，必填资质配置异步加载期间短暂为0个上传控件，程序误走补充资质分支 | 一类医疗器械创建首张资质卡 | 已修复，真实流程通过 | 当当前卡片数为0时仅等待业务自带的主资质表单出现，不点击复选框或新增入口；增加延迟渲染页面夹具回归 |
| BUG-024-26 | 2026-09-09 | 代码假定每个经营业务只有1张内置必填资质卡；“电商代运营”真实配置会自动生成2张不同的必填卡，导致首条输入前数量校验失败 | 电商代运营内置“个人房产行业-承诺函与协议”和“入驻商城备案主体营业执照” | 已修复，真实流程通过 | 按 `.add-reserve` 层级区分内置必填卡与补充卡；等待内置卡数量稳定后按输入目录顺序依次填充，超出内置数量的目录才创建补充资质 |
| BUG-024-23 | 2026-09-09 | 同一卡片3个文件的 `savelicepic` 均成功且页面显示1/3，但移出卡片后20秒内未发起 `submitlice` | 东莞样本推广审查资质1真实上传 | 已修复，真实流程通过 | source map 确认保存绑定在外层 `form-card` 的 `mouseleave`，且需等待上传 success 回填 `filename`；逐张等待预览计数后移出卡片，最长20秒等待 `submitlice` |

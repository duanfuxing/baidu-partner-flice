# 测试场景

- 三个文件依次上传和保存，保存集合长度1、2、3。
- 保存失败停止后续上传。
- 方法不存在或组件歧义停止，不调用别的卡片。
- 保存后卡片重建仍可继续。
- 每次保存验证文件身份、举证及业务状态。

结果：12项方法调用/慢响应/校验错误/定位边界/滚动用例及4项上传保存回归共16场景。组合执行15 passed、1 failed（旧目标业务用例仍期望3文件只保存一次）；修正夹具累积计数和每文件保存断言后该用例1 passed。生产VNode树中卡片重建复测1 passed。compileall及git diff --check通过。

真实页面：同一持续保留的专用Chrome中，确认6个组件仅当前卡片的drag-upload-wrapper包含目标，暴露handleSubmit等5项API；生产代码成功定位并调用当前空表单方法。submitlice和submitall均拦截，无真实文件上传、保存或最终送审。

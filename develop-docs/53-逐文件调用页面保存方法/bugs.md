# 问题记录

- 原来整卡上传完才靠mouseleave触发保存；用户要求改为每文件调用页面自身保存方法。已改为每文件主动调用当前卡片handleSubmit。

- 生产DOM缺少__vueParentComponent，根元素还是拖拽包装层：已用应用VNode树和唯一卡片边界定位，并完成真实空表单拦截验证。
- 原HTML夹具没有真实组件的脏检查，移出时额外重复保存：夹具补齐未变化不保存的行为，并断言保存集合1、2、3与1、2。

- 目标业务旧用例用原生input.files.length判断累积变化并预期整卡一次保存：改为记录累积上传数并断言三文件保存三次，复测通过。

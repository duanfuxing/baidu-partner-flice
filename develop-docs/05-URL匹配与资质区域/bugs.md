# 任务 5：Bugs

| ID | 发现日期 | 问题 | 复现条件 | 状态 | 解决方案 |
| --- | --- | --- | --- | --- | --- |
| BUG-005 | 2026-07-29 | `url.txt` 中的 `https:/...` 少一个斜杠，无法与页面的 `https://...` 精确匹配 | 进入“URL状态概览”后查找目标 URL | 已修复 | 输入清理阶段将常见的 `http:/`、`https:/` 修正为标准双斜杠协议 |
| BUG-006 | 2026-07-29 | 公司流程失败或流程 1-5 完成后，浏览器上下文立即关闭，无法检查页面 | 已进入资质页面后任务返回 | 已被任务 8 替代 | 早期版本曾在交互终端保留 Chrome；多公司独立 worker 上线后统一在成功或失败时关闭 Chrome，并将检查信息持久化到状态 JSON |
| BUG-007 | 2026-07-29 | 页面已显示目标 URL，但程序在“URL状态概览”标题出现后立即查询，异步表格行尚未渲染，误报找不到 URL | 进入真实性资质页面，表格数据晚于区块标题加载 | 已修复 | 等待目标 URL 可见后再匹配；兼容 Element UI 固定操作列，在仅有一个可见“查看”时安全点击 |
| BUG-022 | 2026-07-29 | URL 已处于“待审核”时仍进入行业资质，随后因多个不可删除卡片报结构错误 | 匹配 URL 行的信息资质状态为“待审核” | 已修复 | 点击“查看”前检查匹配行状态；待审核时直接记录明确错误码并跳过当前公司 |

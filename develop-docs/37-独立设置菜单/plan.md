# 任务 37：独立设置菜单

## 目标与范围

侧栏增加设置页面，迁移浏览器类型、程序路径、超时参数。任务中心仅保留任务相关操作。

## 设计与步骤

新增 settings 导航与页面；浏览器和超时各自分组；沿用已实现的保存、校验及任务参数快照。任务执行时仍禁用设置修改。路径错误引导用户到设置页面。

## 依赖

任务 31、33、36；无新依赖。

## 验收标准

设置菜单可见且可切换；任务中心无配置控件；设置持久化不变；最小窗口无重叠、裁切；界面和上传重试工作一并验收。

## 不在范围内

不打包发布，不变更业务逻辑。

## 验收结果

已完成。侧栏新增设置，浏览器类型/路径/超时已迁入独立页面，保留缓存与运行时禁用逻辑；默认及最小窗口实际 Tk 几何和页面切换检查通过。相关 GUI、设置、浏览器回归通过。

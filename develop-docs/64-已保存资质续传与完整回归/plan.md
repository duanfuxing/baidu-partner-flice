# 已保存资质续传与完整回归

## 目标与证据
任务61已在真实页面保存推广审查资质1、资质2各3个文件。当前正式流程恢复推广审查编辑页后，仍执行拒绝非空补充草稿的初始化逻辑，并从第一个文件重新上传，无法处理已保存数据。结合已核验的远端状态和源代码可确认阻断，无需再消耗一次浏览器运行重现。

## 设计
- 仅在程序明确打开“已保存待送审”推广审查编辑页时开启续传；新增页的非空草稿保护保留。
- 页上已有卡片不得超过输入目录数；逐卡按顺序匹配输入，已有文件必须是对应目录文件的完整或部分前缀。
- 用已有预览文件ID、加载状态、源文件SHA-256与从当前预览URL读回的内容验证；非保存状态、文件数量超限、内容或顺序不符时停止，不删除或覆盖资料。
- 已有文件验证通过后复用其身份，举证不符则由当前页面保存方法修正；完整一致则不重复保存。仅上传缺失文件，随后按原有保存及回填逻辑验证。
- 结果仍纳入完整集合校验。测试运行期间只观察，不临时修正。

## 步骤与验收
1. 明确续传入口，增加既有文件前缀与持久化状态验证。
2. 本地Chrome测试完整复用、部分续传、错误内容/顺序/数量/保存状态拒绝及举证修正。
3. 联同任务63从正式入口执行全输入实测，失败时在该轮结束后修复再重测。

## 范围与依赖
依赖任务61、63，无新依赖。禁止最终送审，不打包，不清理用户输入或认证状态。

## 状态
已完成，最终全量验证见任务66。

第二轮19:39:51–19:44:07：URL竞争由程序自动重查恢复；页面走新增分支，未进入续传。第8张文件保存成功后当前卡片刷新丢失，30秒内未恢复，退出码1。运行期间源码一致、无人工干预，见 round-2.log。当时尚未完成续传验收；第三至五轮随后验证通过，见测试记录。

最终验收：任务66第五轮全量成功，4类业务11份资质19文件核验通过，未送审。测试全程无人工干预。见 [完整验证](../66-空业务页新增卡片兼容/verification.md)。

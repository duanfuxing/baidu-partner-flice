# 任务 32：0.30.8 版本发布

## 目标与范围

提交浏览器选择、路径缓存和缺失提示改动，以 0.30.8 发布四架构安装包。

## 设计与步骤

核对远程标签；同步源码、项目元数据、Windows/macOS 打包版本及测试；执行相关回归；提交并推送 master 和 v0.30.8 标签；确认 Actions 对应发布提交。

## 依赖

任务 30、31 和现有 GitHub Actions；无新增依赖。

## 验收标准

版本统一为 0.30.8，相关测试通过，标签指向发布提交，正式打包已触发。

## 不在范围内

不覆盖旧标签，不改写用户资料；Windows 真机与不同 Chromium 浏览器的业务兼容性仍需手工验收。

## 验收结果

52 项相关测试通过，版本配置已统一为 0.30.8；准备推送标签。云端构建结果及真机安装待后续验收。

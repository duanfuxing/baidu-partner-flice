# 任务 25：测试用例

| 编号 | 场景 | 预期结果 |
| --- | --- | --- |
| T25-01 | 保存有效输入目录 | `cache/settings.json` 原子写入绝对路径 |
| T25-02 | 读取有效缓存 | 返回规范化目录路径 |
| T25-03 | 缓存文件不存在或 JSON 损坏 | 返回空，不抛出异常 |
| T25-04 | 缓存路径不存在或不是目录 | 返回空，不回显失效路径 |
| T25-05 | 再次选择目录 | 覆盖为最新目录 |
| T25-06 | 任务执行结束 | 保留目录文字、清除旧验证结果，必须重新验证才能运行 |
| T25-07 | macOS `.app` 重启 | 任务中心回显上次有效目录，最终提交开关仍未勾选 |
| T25-08 | 检查任务中心、导航和按钮 | 页面为灰白背景、白色卡片、浅蓝强调，未出现原深色侧边栏 |

## 执行结果

- 2026-09-09：缓存及 GUI 针对性测试全部通过。
- 2026-09-09：最终代码完整 `pytest` 回归通过，共 153 项。
- 2026-09-09：重新构建并启动 `dist/百度资质自动提交工具.app`；进程和主窗口均正常创建，版本 0.30.6，Mach-O x86_64。

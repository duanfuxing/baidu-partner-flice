# 修复验证记录（2026-09-12）

## 本地验证

共 155 项相关测试分批通过：保存主动调用 12 项、保存回填 18 项、流程 6 页面回归 60 项、新版业务校验 19 项、文件身份 7 项、保存全程观察 30 项、诊断采集 9 项。页面测试使用本地 HTML 和路由模拟的无头 Chrome，不连接真实最终送审接口。

新增覆盖：自动保存早于保存入口；保存进行中；错卡/旧时间边界/少文件/错误举证；最新失败不得被旧成功掩盖；HTTP/业务/非 JSON 错误；响应丢失后的详情核对；内部 crmid 映射及错误映射拒绝；三文件完整流程；第二文件保存失败时禁止上传第三文件；回填核验期间迟到保存覆盖旧结果时停止。

## 真实验证

复用已保存登录状态，仅启动一个项目专用 Chrome；在同一会话完成原记录检查、新资质上传、页面重新打开、只读详情接口核对、组件和事件审计，最后结束会话。

样本：输入中“推广审查 / 资质2”的 3 个文件。保留此前“资质1”的 3 个文件，未删除或覆盖。初始采集配置的 sample_files 对应被保留的资质1；本轮上传样本的哈希以最终 verification.json 为准。

| 文件 | 上传请求 / 耗时 | 保存请求 / 耗时 | 结果 |
| --- | --- | --- | --- |
| 1 | r186 / 291 ms | r191 / 328 ms | HTTP 200，status=0 |
| 2 | r199 / 231 ms | r204 / 221 ms | HTTP 200，status=0 |
| 3 | r211 / 405 ms | r217 / 316 ms | HTTP 200，status=0 |

从首文件操作标记到生产单卡上传函数成功返回共 26.034 秒。共 3 次上传、3 次保存，无重复上传或保存。此窗口共 43 个浏览器请求，包含页面资源与程序文件内容核验请求。

重新打开后，两张资质卡均各有 3 个已加载文件、状态“已保存待送审”；新上传和原保留卡的文件内容 SHA-256、文件 ID 与顺序、举证链接全部核验通过。随后直接复用已观察到的 GET 详情 URL，调用正式只读核对实现，返回匹配成功。

接口核对首次失败暴露了同名 crmid 字段语义不同，已修复映射并在同一浏览器中再次确认成功。这进一步说明直接写接口前不能仅凭同名字段拼参数。

未触发 `submitall`，未最终送审。全会话采集到 288 个浏览器请求、1640 个事件，无 operation_error、session_error 或被拦截写入。初始 URL 表格初始化竞争通过现有诊断中的一次只读重试完成，没有重启 Chrome。

## 验证边界

此次真实样本三个保存均由保存入口之后发出，未再次出现旧现场的提前自动保存；提前保存竞态由本地 Chrome 三文件夹具稳定复现并验证修复。真实百度断网/5xx 未作故障注入；这些异常分支在本地测试。纯接口上传/保存未作真实写入验收，默认仍走页面提交。

原始脱敏证据位于忽略目录 `logs/qualification-trace-20260912-190651/`：`events.jsonl`、`request-index.json`、`timeline.md`、`verification.json`、`page-methods.json`、`card-events.json`。不提交认证信息或原始请求头。

## 会话收尾

验证结束后 `context.close()` 已完成，但诊断进程等待 `browser.close()` 返回时停滞；在所有上传、保存与只读验证完成后中断该诊断进程，退出码为 1（KeyboardInterrupt）。随后通过进程列表确认没有 Chrome、Playwright 或本次诊断 Python 残留。此退出码不代表资质上传失败；关闭等待原因尚未定位，作为独立运行环境问题记录。本次没有强杀正在提交的 worker。

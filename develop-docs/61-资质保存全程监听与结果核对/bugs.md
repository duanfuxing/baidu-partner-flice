# 问题记录

| 编号 | 问题 / 复现条件 | 状态 | 解决方案 |
| --- | --- | --- | --- |
| 61-01 | 第三张自动保存成功后程序才监听，页面脏检查不再发新请求，程序报超时 | 已修复并验证 | 从上传前观察，按本卡累积文件/举证和业务身份核对保存结果 |
| 61-02 | 本地 Chrome 在沙箱内启动被系统 SIGABRT 中止 | 环境限制，已重跑 | 使用已授权的 pytest 沙箱外执行，本地路由拦截全部网络 |
| 61-03 | 真实详情记录的内部 crmid 与请求 crmid 不同，按同名字段直接比较导致只读核对未命中 | 已修复并验证 | 通过同一响应中 url_info.urlid 与请求 urlid 核对后，使用 url_info.crmid 关联记录内部 crmid；仍要求其他身份、文件和举证全部一致 |
| 61-04 | 验证结束 context.close() 已返回，browser.close() 等待停滞 | 收尾已处理，原因待定位 | 完成全部核验后中断诊断进程；进程检查无 Chrome/Playwright/诊断 Python 残留，不影响已验证的资质记录 |

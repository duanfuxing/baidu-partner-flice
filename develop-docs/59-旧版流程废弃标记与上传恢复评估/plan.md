# 任务 59：旧版流程废弃标记与上传恢复评估

## 目标与范围

按用户要求标记旧版 `/flice` 行业资质逻辑废弃，当前有效业务流程为新版 `/newaudit`。检查现有上传和保存实现，提出弱网及慢响应情况下的恢复方案。

## 设计与实现步骤

1. 在项目说明、开发约定、基础需求、历史任务及旧版代码入口标明废弃状态，保留历史代码。
2. 同步新版现状：每个资质目录一张卡片，目录内逐文件上传；每张上传后调用卡片保存方法并核验回填。
3. 记录现有重试边界和建议：分阶段超时、结果未知时先核对远端状态、上传与保存分别恢复、退避重试、逐文件检查点。
4. 检查文档及差异格式；不将建议写成已实现功能。

## 依赖

依赖任务 23、46、48、53、55；无新增依赖。

## 验收标准

- [x] 当前说明明确旧版废弃，历史实现不再作为有效操作依据。
- [x] 新版目录、文件、保存规则与当前代码一致。
- [x] 建议区分现有能力与待实现内容，注明尚未定位本次实际失败根因。
- [x] `git diff --check` 通过。

## 不在范围内

删除旧版代码或改变运行分支、实现新的恢复机制、连接真实资质页面、上传或最终送审、打包发布。

## 状态

已完成。此次仅修改文档、模块说明和分支注释，未改变运行逻辑；通过差异检查，未运行浏览器或业务测试。恢复建议见 `recovery-proposal.md`，真实网络、百度服务及只读详情接口能力尚未验证，恢复机制未实现。

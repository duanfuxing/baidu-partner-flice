# 回归测试

- 正常上传：一次完成，成功图片和当前图片均存在。
- 404预览、HTTP200内容错误：只删除当前坏项，然后第二次上传成功；日志含预览HTTP状态。
- HTTP5xx和HTTP200业务失败：清理失败占位项后重传，成功项保留。
- 重试耗尽：共4次上传，4个失败项均清理，当前公司失败。
- 删除按钮无效：停止，不开始下一次上传。
- 多个多余项：不能唯一确定失败项时停止，不误删。
- 上传请求中断：结果未知，立即停止，不重传。
- 每次重传前断言仅保留原有成功项，原图片内容哈希一致。
- 回归：旧版上传、新版保存后预览检查、文件身份及内容核验、URL搜索。

首轮21项相关回归通过（119.85秒）；新增业务失败用例及完整250项回归全部通过（250.57秒）。真实页面登录状态失效，本次只使用本地HTML页面与模拟接口测试，未修改真实业务数据。

## 复查新增回归

先复现3个失败用例，再修复验证：读取成功图片期间列表插入其他条目时不得误删；内容核验期间新增上传请求时不得判成功；清理期间新增上传请求时不得删除或重传。

修复后，失败恢复、URL纯页面搜索、文件身份核验共32项测试通过（64.23秒）。新版整卡保存及原上传重试兼容14项测试通过（82.55秒）；本轮共46项针对性回归通过。

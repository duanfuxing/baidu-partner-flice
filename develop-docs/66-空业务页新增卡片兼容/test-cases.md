# 测试记录

新增集合、默认卡复用、编号变化、非空拒绝和正常新增场景待验证。

本地 Chrome：7 个默认组场景及原单卡新增回归共 8 项通过。覆盖隐藏非活动卡、删除后重排编号、非空/已保存/缺表单/歧义拒绝。开始第四轮完整实测。

补充固定默认组场景，共9项通过。开始第五轮完整实测。

第五轮完整实测成功：4类业务、11份资质、19文件通过，退出码0；无运行中干预，未最终送审。详见 [验证记录](verification.md)。

最终回归：`tests/test_new_audit_qualification.py` 19项、`tests/test_playwright_flow6.py` 60项，共79项全部通过；空默认业务组新增专项8项另行通过（连同原单卡新增共9项）。`git diff --check`通过。未打包、提交或发布。

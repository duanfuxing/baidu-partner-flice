# 最终完整验证

2026-09-12，第五轮从 `main.py --no-final-submit` 正式入口运行成功，进程退出码0。输入、公司搜索、资质链接、URL精确匹配、全部资质处理和最终集合校验自动完成，浏览器自动关闭。

| 范围 | 结果 |
| --- | --- |
| 公司 | 东莞山虞医药有限公司，1家公司 |
| 推广审查 | 3份资质、9文件；全部内容和顺序核验后复用，无重复上传 |
| 一类医疗器械 | 2份资质、4文件；新增并逐文件上传保存 |
| 二类医疗器械 | 3份资质、3文件；新增并逐文件上传保存 |
| 电商代运营 | 3份资质、3文件；新增并逐文件上传保存 |
| 合计 | 4类业务、11份资质、19文件全部核验通过 |
| 调度结果 | success，queued/running均空 |
| 最终审核 | 按选项跳过，未点击发起审核 |
| 运行中人工操作 | 0，main.py及src全部文件运行前后哈希一致 |

运行时间20:06:23–20:09:54；公司worker状态20:06:33–20:09:54。见 [脱敏日志](round-5.log) 和 [结果摘要](round-5-result.json)。

结果文件中的11个资质项目均success=true，CLI qualification_submission_completed=true。`qualification-submit-result.json`顶层success按现有定义包含“最终送审成功”，本轮主动不送审，因此该顶层值为false；公司调度success表示本次选项要求的全部操作完成。

## 修复覆盖与证据边界

- URL查询竞争：第二、四轮真实出现并自动恢复；初始入口重载分支本地验证。
- 已保存续传：第三轮真实复用8个文件并补1个；第四、五轮复用完整9文件。
- 保存后详情读取：第五轮所有读取正常并驱动页面回填；1009/5xx/断网重试由本地HTTP服务与Chrome验证，未在成功全程中注入百度故障。
- 空业务重新新增：第四轮证实生成4张固定默认卡；第五轮自动再新增可编辑业务、精确选型、清理空默认卡，随后完成其他3类业务。
- 本次全程包含恢复已有数据，不是19个文件从全空页面重传；没有为测试清空已保存资质。
- 纯接口上传/保存仍未替换默认方案，使用页面提交与接口结果判断组合。

## 每轮结果

1. 入口页面未加载，自然失败；补充有限入口重试。
2. 第8个文件已保存但详情回填失败，自然失败；补充详情只读恢复及严格续传。
3. 推广审查9文件完成，空业务新增数量判断失败。
4. 确认从0新增生成4默认卡，因无可编辑类型停止。
5. 全流程成功。每轮测试只观察，代码修复均在上一轮结束后完成。

最终回归：`tests/test_new_audit_qualification.py` 19项、`tests/test_playwright_flow6.py` 60项，共79项全部通过；空默认业务组新增专项8项另行通过（连同原单卡新增共9项）。`git diff --check`通过。未打包、提交或发布。

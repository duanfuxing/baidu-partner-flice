# 任务 23：问题记录

| 编号 | 日期 | 问题 | 复现条件 | 状态 | 解决方案 |
| --- | --- | --- | --- | --- | --- |
| BUG-023-01 | 2026-09-04 | 误将新版入口理解为“新增产品资质” | 新增页同时展示产品和业务入口 | 已纠正规则 | 必须点击蓝色“新增业务资质”，禁止点击产品入口 |
| BUG-023-02 | 2026-09-04 | 页面默认生成业务1–4，多余默认业务会污染最终集合 | 进入新增业务资质页 | 规则已确认 | 保留不可删除的推广审查业务1，从后向前删除业务2–4 |
| BUG-023-03 | 2026-09-04 | 多个输入类型不能共用一个业务卡片 | `qualificationTypes` 包含多个类型 | 规则已确认 | 推广审查复用业务1，其余类型各新增一个业务卡片 |
| BUG-023-04 | 2026-09-04 | 新版每个上传表单只处理一张实际文件 | 同一类型包含多张文件 | 规则已确认 | 第一张使用主表单，其余逐张创建补充资质，类型保持“其它” |
| BUG-023-05 | 2026-09-04 | 文件上传和举证链接分别触发自动保存 | 填写当前资质区域 | 规则已确认 | 每次操作分别等待对应 `submitlice` 成功响应并回读页面 |
| BUG-023-06 | 2026-09-04 | “发起审核”确认后的成功判据尚不完整 | 点击确认弹窗中的“确定” | 已解决 | 等待页面触发 `/permit/web/permit/submitall`，仅 HTTP 200 且 `status == 0` 视为成功 |
| BUG-023-07 | 2026-09-04 | 输入加载阶段强制校验旧版字段会阻断新版流程 | 新版只提供文件和举证链接 | 已修复 | 新版允许编号、名称、有效期缺省；旧版在页面修改前单独校验完整字段 |
| BUG-023-08 | 2026-09-04 | 多业务表单使用全页面控件集合，可能串业务上传 | 页面同时展示多个业务卡片 | 已修复 | 为业务标签解析对应内容容器，所有表单操作限定在当前容器 |
| BUG-023-09 | 2026-09-04 | 推广审查校验可能命中页面说明文字 | 业务1类型异常且说明区域包含推广审查 | 已修复 | 仅从业务1内容容器读取经营业务值 |
| BUG-023-10 | 2026-09-04 | 新版缺省编号和名称导致结果键冲突 | 同一类型存在多个资质目录 | 已修复 | 结果增加稳定的资质目录标识并用于幂等匹配 |
| BUG-023-11 | 2026-09-04 | 下拉候选异步渲染时被立即判定为不存在 | 输入经营业务查询词后候选延迟出现 | 已修复 | 在超时范围内轮询唯一精确候选 |
| BUG-023-12 | 2026-09-04 | `submitall` 接受非 200 的其他 2xx | 接口返回 201/204 | 已修复 | 严格判断 `response.status == 200` |
| BUG-023-13 | 2026-09-04 | 隐藏文件 input 被可见性过滤 | Element UI 上传组件隐藏原生 input | 已修复 | 在可见业务容器内读取全部文件 input，不要求 input 自身可见 |
| BUG-023-14 | 2026-09-04 | 完成结果未比较举证链接 | 最终成功后仅修改举证链接 | 已修复 | 持久化并比较完整公司输入指纹 |
| BUG-023-15 | 2026-09-04 | 推广审查仍可能命中业务卡说明文字 | 说明中含推广审查但经营业务值不同 | 已修复 | 读取明确经营业务控件或字段对应展示值 |
| BUG-023-16 | 2026-09-04 | 最小祖先容器可能不包含补充资质区 | 主表单和补充操作分属不同子节点 | 已修复 | 容器候选优先要求覆盖补充资质入口及业务表单 |
| BUG-023-17 | 2026-09-04 | 删除业务回退可能点击无语义 SVG | 标签同时包含状态和关闭图标 | 已修复 | 移除任意 SVG 回退，只接受关闭/删除语义和确认类名 |
| BUG-023-18 | 2026-09-04 | 单项保存完成后未重新校验最终业务和表单集合 | 页面在保存后异步丢失业务或表单 | 已修复 | 最终送审前重扫业务、表单、保存凭据和举证链接，任一不一致禁止送审 |
| BUG-023-19 | 2026-09-04 | 保存响应只按 URL 子串匹配，可能错误归属其他请求 | 页面同时出现相似路径、其他域名或非 POST 请求 | 已修复 | 严格匹配 HTTPS、百度域名、精确接口路径和 POST 方法 |
| BUG-023-20 | 2026-09-04 | 缺少完整指纹的历史结果仍可能跳过已变化输入 | 旧结果无 `input_fingerprint` 且只修改举证链接 | 已修复 | 无完整指纹一律不视为安全命中 |

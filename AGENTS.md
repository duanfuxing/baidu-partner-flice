# AGENTS.md

## 项目目标

本项目使用 Python + Playwright 控制本机 Chrome，读取 `input` 中的公司资质数据，进入百度客户资质系统并完成自动化操作。

需求流程 1–5 已实现并完成基础联调：

1. 数据格式化与验证；
2. 打开百度营销代理商平台并获取/复用登录状态；
3. 搜索公司并获得唯一 `custId`；
4. 获取资质流程链接，点击“资质环节”进入提交页面；
5. 按 URL 匹配页面记录，点击“查看”，确认“行业资质”区域。

流程 6“经营业务清理与资质批量提交”已实现并完成真实页面联调，包含处理页面已有目标类型、清理多余经营业务、搜索添加页面缺失类型、上传文件、处理 OCR 提示、填写表单和单个资质提交。所有单项和最终集合校验通过后，根据桌面任务的“完成后执行全部提交”选项决定是否点击页面“本模块提交/全部提交”；启用时必须校验 `submitall` 成功，未启用时停在资质填写完成状态，然后自动关闭 Chrome。多公司固定使用 1 个独立 Playwright/Chrome worker 串行处理，默认不允许并发提交。

## 开发环境

- Python 版本：3.11.8。
- 项目虚拟环境：`.venv`。
- 激活环境：`source .venv/bin/activate`。
- 业务代码目录：`src/`。
- 项目根目录入口：`main.py`。
- 依赖清单：`requirements.txt`；打包和开发依赖约束同步维护在 `pyproject.toml`。
- 使用虚拟环境中的 `python` 和 `pip`，不要依赖系统 Python 的全局包。
- 浏览器自动化使用 Playwright，目标浏览器为本机安装的 Chrome。
- 不提供 `--workers` 参数；任意公司数量均固定使用 1 个 worker 串行处理。
- 新增依赖前先确认是否已有等价依赖；依赖变更需同步记录到项目依赖文件。

## 需求和文档规范

- `docs/需求.md` 是流程和目录结构的基础需求文档。
- `docs/公司搜索接口.md` 和 `docs/获取资质链接接口.md` 是流程 3–4 的接口依据。
- `docs/资质提交.md` 和 `docs/资质文件上传.md` 是流程 6 的接口参考；页面操作和接口响应需要结合真实页面联调验证。
- `docs/全部提交.md` 是最终送审接口依据；必须通过页面按钮触发，不自行拼接其中的动态请求参数。
- 接口真实字段名必须保持原样，例如 `qulificationAuditUrl`，不能因为拼写看起来不规范而自行改名。
- 不把 Cookie、Token、账号、密码或其他敏感值写入源码、计划文档、普通日志或截图文件名。现有接口示例中的敏感值只可视为脱敏/测试资料，开发时不得复制使用。

## 开发任务文档规范

每一次开发任务必须建立在 `develop-docs` 下的独立子目录中，目录名使用有序编号和任务名称，例如：

```text
develop-docs/
└── 01-数据格式化与验证/
    ├── plan.md
    ├── test-cases.md
    └── bugs.md
```

要求：

- `plan.md`：记录目标、范围、设计、实现步骤、依赖、验收标准和不在范围内的内容。
- `test-cases.md`：记录正常、异常、边界和回归测试场景。
- `bugs.md`：记录问题、复现条件、状态和解决方案；没有问题时保留空模板。
- 开发开始前先更新对应 `plan.md`；发现问题立即更新 `bugs.md`；修复后补充回归用例。
- 完成任务前检查计划中的验收标准，并在文档中记录未完成项或环境限制。
- `develop-docs/README.md` 维护任务顺序、依赖关系和整体状态。

## 输入数据规则

输入目录结构为：

```text
input/
└── 公司名/
    ├── url.txt
    ├── input.json                 # 校验通过后自动生成
    └── 资质类型/
        └── 资质目录/
            ├── 表单信息.txt
            └── 图片或 PDF 文件
```

- 每个公司目录只有一个小写文件名 `url.txt`。
- URL 读取后必须移除空格、换行和制表符，并将常见的 `http:/`、`https:/` 单斜杠修正为标准协议。
- 每个资质目录有一个固定格式的 `表单信息.txt`，字段为：资质编号、资质名称、有效期至、举证链接。
- `有效期至：永久` 表示页面勾选“永久”；日期使用 `YYYY-MM-DD`，直接粘贴到日期输入框，禁止操作日期选择面板。
- `举证链接` 非必填。
- 资质文件只允许 jpg、jpeg、png、pdf；其他文件忽略。
- 百度页面限制最多 9 个文件、单个文件不超过 10MB，数据校验阶段提前检查。
- 一个公司可以有多个资质类型，每个类型可以有多个资质目录。
- 同一个资质类型内允许资质编号重复；资质以“资质编号 + 资质名称”作为联合识别键。只有编号和名称同时重复时才视为无法安全区分的重复输入。
- 所有公司数据校验通过后，必须在对应公司目录生成 `input.json`。
- `input.json` 使用 UTF-8、`ensure_ascii=false` 和 2 个空格缩进保存；其中资质文件路径相对于公司目录保存。
- 任意输入校验失败时，不生成或更新 `input.json`，不得带着未校验数据进入浏览器流程。

## 浏览器和登录规范

- 使用 Playwright 控制本机 Chrome，使用非无头模式。
- 不控制用户日常 Chrome 的默认个人资料；使用项目专用的浏览器上下文/登录状态文件。
- 首次运行或登录状态失效时，打开工作台并暂停，提示用户人工完成登录。
- 不自动处理账号密码、短信验证、验证码或安全验证。
- 登录成功后保存 Cookie 和 localStorage 等 storage state，后续运行自动加载。
- 桌面程序人工确认登录后必须再次通过 URL 和页面特征验证；仍在登录页时不得保存登录状态。
- 建议登录状态保存到 `.auth/storage_state.json`，该目录必须被忽略，不得提交。
- 判断登录成功必须结合 URL 和页面特征，不得仅依赖单个易变化的 CSS 类名。
- 认证信息失效时暂停重新登录，成功后覆盖保存状态并重试当前可重试步骤。

## 接口调用规范

### 公司搜索

```text
POST https://partner.baidu.com/polaris-web/onecrm-oneFrontArchive/archive/cust/list/query
```

- `custName` 使用公司一级目录名称。
- 校验响应 `code == 0`。
- 使用 `data.totalCount` 判断结果数量。
- 只有唯一结果时提取 `data.result[0].custId`。
- 0 条或多条结果都属于业务错误。

### 获取资质链接

```text
GET https://partner.baidu.com/polaris-web/onecrm-oneFrontArchive/archive/cust/summary/query?custId={custId}
```

- 使用公司搜索得到的动态 `custId`。
- 校验响应 `code == 0`。
- 提取 `data.qulificationAuditUrl`。
- 接口请求应复用已登录浏览器上下文的 Cookie。

## 页面流程规范

1. 打开 `https://partner.baidu.com/portal/workbench` 并完成登录检测。
2. 调用公司搜索接口。
3. 调用资质链接接口。
4. 在同一浏览器上下文中新页面打开资质链接。
5. 点击页面中的“资质环节”。
6. 最终 URL 的域名应为 `fkzhunru.baidu.com`、路径为 `/flice`，Hash 以 `#/truth/submit` 开头；允许 HTTP/HTTPS 和 Hash 前的动态查询参数，动态参数不可硬编码。
7. 等待“URL状态概览”表格加载。
8. 按清理后的输入 URL 与页面 URL 列进行精确匹配。
9. 在匹配行内部点击“查看”，不得点击页面中第一个同名按钮。
10. 以“行业资质”区域出现作为流程 5 成功标志。

## 经营业务与资质提交规范

- 流程 6 开发前阅读 `develop-docs/06-经营业务清理与资质批量提交/plan.md`、`type-mapping.md` 和 `test-cases.md`。
- 先读取页面全部经营业务并完成输入类型映射预检，再执行任何删除；不得使用模糊包含关系决定破坏性删除。
- 页面显示名与输入类型名不完全一致时，必须使用经过确认的显式映射。存在未知类型、歧义或同名多项时停止当前公司，保留页面供人工检查。
- 输入需要但页面默认不存在的经营业务，通过“经营业务”下拉框按输入类型精确搜索；唯一候选项与显式映射一致时才允许选择并点击“添加”。
- “一类医疗器械”应添加为“一类医疗器械（不可个人，B）”，不得映射成“入驻商城类（不可个人，C2）”。
- 新增经营业务后必须重新扫描页面，确认新增类型唯一出现且原有目标类型仍存在。
- 只删除明确不在输入需求内且页面提供“删除业务”操作的经营业务；系统强制保留且不可删除的类型按保留项处理。
- 删除应从页面靠后的业务开始，每删除一项后重新扫描页面，避免业务序号变化导致误删。
- 每个保留经营业务均按 `input.json` 做整类覆盖，不复用或跳过页面已有资质。
- 已填资质中有删除入口的卡片全部从后向前删除；每次删除前核对编号和名称，删除后重新扫描。
- 每种经营业务唯一不可删除的首张资质卡必须保留，并用 `input.json` 中该类型第一条资质编辑覆盖；如果首卡为空，则直接在该卡填写第一条资质。
- `input.json` 中其余资质一律通过“上传备用资质文件”作为新资质上传。若存在多个不可删除的已填卡或无法确认首卡，停止当前公司。
- 新增经营业务后，系统第一张必填资质可能直接显示为面板内嵌表单而非弹窗；检测到完整上传控件、字段和提交按钮时，应直接在该卡片内填写，不得继续等待 `.el-dialog`。
- 资质文件按 `input.json` 中的顺序上传，仅允许已经通过输入校验的图片和 PDF。
- 上传后如出现 OCR 识别提示，点击“好的”关闭；无论 OCR 是否成功，都必须清空并以 `input.json` 的资质编号、资质名称、有效期和举证链接为准重新填写。
- OCR 可能延迟回填，提交前必须再次读取表单值并与 `input.json` 比对；不一致时不得提交。
- 每次单个资质提交后等待弹窗关闭及异步卡片完整渲染，连续确认编号、名称、有效期、举证链接和文件数量一致后，才能开始下一项。
- 上传接口返回的服务端文件 ID 用于审计；页面缩略图 URL 格式可能不同，提交后以接口成功、页面字段及文件数量为准。
- 任一单个资质失败时立即记录并停止当前公司，直接输出该资质的原始错误；不得在页面状态未确认稳定时继续下一项。
- 所有公司资质单项提交、页面卡片和最终集合均验证成功后，根据任务选项决定是否点击唯一可见的“本模块提交/全部提交”；启用最终提交时校验 `submitall` HTTP 成功且 `status == 0`，未启用时明确记录跳过最终提交。
- 任一公司处理失败时不得执行该公司的 `submitall`；每个 worker 成功或失败后都关闭自己的 Chrome，并将结果写入调度状态。

## 多公司调度规范

- 主进程解析并持久化全部输入后，只启动一次登录 Chrome，完成登录检测/人工登录并保存 `.auth/storage_state.json`，随后立即关闭。
- worker 使用独立进程、独立 Playwright、独立 Chrome 和独立浏览器上下文，只读取主进程保存的登录状态，不重复访问登录检测流程，也不得改写登录状态文件。
- 默认禁止并发提交：存在待处理公司时只启动 1 个 worker，所有公司按调度顺序串行处理。
- 统一状态文件为 `input/scheduler.json`，公司状态文件为 `input/<公司>/execution-status.json`。
- URL 状态概览中目标行的“信息资质状态”为“待审核”时，不得进入详情或修改资质；公司标记为 `failed`，写入 `errorCode: qualification-pending-review` 后继续调度下一家公司。
- 状态至少包含 `queued`、`running`、`success`、`failed`、`dry-run-success`；统一文件分别维护 `queuedCompanies` 和 `runningCompanies`。
- 调度 JSON 必须通过 `fcntl` 文件锁和临时文件原子替换更新；存活进程已预留或执行的公司禁止被另一主程序重复领取。
- 协调进程或 worker PID 已不存在时，其陈旧状态允许下一次运行回收。
- 相同输入已有 `final_submission_success=true` 且文件哈希一致时，直接标记完成并跳过，不得重新打开已锁定的送审页面或重复 `submitall`。

## 错误处理规范

- 可恢复的网络错误、超时和 5xx 错误可进行有限次数重试。
- 公司不存在、搜索结果不唯一、URL 不存在或匹配多行属于业务错误，不应盲目重试。
- 默认记录当前公司错误并继续下一个公司。
- 桌面程序取消任务时不得强杀正在提交的 worker；当前公司稳定结束后停止后续公司，未开始公司记录 `task-cancelled`。
- 登录失效应暂停任务，提示人工登录，不把登录页当作业务页面继续执行。
- 页面定位、跳转和表格加载失败时保存脱敏错误信息，必要时保存页面截图；截图不得包含未授权的敏感信息。
- 错误日志不得包含 Cookie、Token、完整请求头或密码。

## 测试规范

- 数据解析优先编写单元测试，覆盖正常、缺失字段、非法日期、永久有效期、空 URL、文件类型和文件大小边界。
- 接口调用应覆盖成功、0 条、多条、业务错误、未授权、超时和响应结构异常。
- 页面自动化测试优先使用稳定的文本/语义定位，XPath 作为已验证页面结构的备用定位；动态参数不能写死。
- 流程 1–5 的真实页面验收终点是“行业资质”区域出现。
- 流程 6 的自动化测试默认使用页面夹具或模拟接口；必须分别覆盖单个资质 `submitlice` 和最终 `submitall`，自动测试不得连接真实最终送审接口。
- 流程 6 的页面定位、上传、OCR、永久选项和提交必须由本地 HTML + 无头 Chrome 的 Playwright 测试覆盖；纯数据单元测试不能替代页面夹具测试。
- 需要真实登录的测试应标记为手工验收，不在无人值守测试中保存或输出真实认证信息。

## 代码和变更规范

- 默认运行命令为：`python main.py`，默认读取根目录下的 `input/`。
- 可通过 `python main.py --input <目录>` 指定其他输入目录。
- 主登录 Chrome 保存状态后自动关闭；单 worker 无论成功或失败都关闭自己的 Chrome。`--no-wait` 仅保留为兼容参数。
- 运行测试使用：`pytest`；安装依赖使用：`pip install -r requirements.txt`。
- 变更前先阅读相关需求和任务计划，保持单个任务范围清晰。
- 优先使用小而清晰的模块：输入解析、会话管理、接口客户端、页面导航和 URL 匹配分别隔离。
- 不在代码中硬编码当前公司的 `custId`、URL、Token 或动态页面参数。
- 读取文件时明确指定 UTF-8，并正确处理中文路径。
- 对文件路径使用 `pathlib`，避免手写平台相关分隔符。
- 完成代码后运行相应测试、静态检查或最小手工流程，并更新任务文档。
- 不执行破坏性删除、覆盖用户输入资料或清理浏览器登录状态的操作，除非获得明确要求。
